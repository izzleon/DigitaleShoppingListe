<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head'); %>
</head>

<body class="bg-gray-300 select-none">

    <%- include('./partials/nav'); %>

        <br class="mt-10">

        <div class="container mx-auto">
            <% for(var i=0; i < eikaeufe.length; i++) { %>
                <div class="bg-gray-100 mx-2 my-4 p-4 shadow-xl">

                    <!-- Fertiger Einkauf -->
                    <% if(eikaeufe[i].sum){ %>
                        <div class="flex justify-between items-center">
                        <p>
                            Einkauf vom <%= eikaeufe[i].date_end.toLocaleDateString('de-DE'); %>
                        </p>
                        <p class="text-lg">
                            € <%= eikaeufe[i].sum %>
                        </p>
                        </div>

                     <!-- Offener Einkauf -->
                     <% } else{ %>  
                        <svg class="w-8 h-8 text-purple-700" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v3m0 0v3m0-3h3m-3 0H9m12 0a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                      <% } %>

                </div>
                <% } %>
        </div>

</body>

</html>